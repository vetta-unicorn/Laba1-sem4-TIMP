#include "PageV1.cpp"
#include <map>
template <typename T>
class BookV1
{
private:
	std::map<size_t, Page<T>> book;

public:
	void CreateBookFile(std::string fileName)
	{
		std::ofstream fileOut ()
	}
};